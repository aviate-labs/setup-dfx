on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ./
        with:
          dfx-version: 0.7.1
          vessel-version: 0.6.1
      - run: dfx --version
      - run: moc --version
      - run: vessel --version
